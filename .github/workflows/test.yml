name: test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: echo ${{ vars.TEST_VAR }}
      - run: echo ${{ secrets.TEST_SECRET }}
      - run: npm ci
      - run: npm test
